import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */

  output: 'export',
  basePath: '/brahmma', // Replace with your actual repo name
  assetPrefix: '/brahmma',

  
};

export default nextConfig;
